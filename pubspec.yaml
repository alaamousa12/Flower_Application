name: quiz_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  # --- تم دمج المكتبات من الطرفين هنا ---
  cupertino_icons: ^1.0.2
  http: ^1.2.0
  provider: ^6.0.5
  font_awesome_flutter: ^10.7.0
  # ------------------------------------

  shared_preferences: ^2.5.3
  lottie: ^3.1.0
  step_progress_indicator: ^1.0.2
  image_picker: ^1.2.0
  animate_do: ^3.0.2
  iconly: ^1.0.1
  flutter_zoom_drawer: ^3.2.0
  google_fonts: ^6.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/animations/
    - assets/icons/